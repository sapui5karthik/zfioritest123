_schema-version: "3.2"
ID: zsalesorderfioriapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zsalesorderfioriapp-destination-content
  type: com.sap.application.content
  requires:
  - name: zsalesorderfioriapp-destination-service
    parameters:
      content-target: true
  - name: zsalesorderfioriapp-repo-host
    parameters:
      service-key:
        name: zsalesorderfioriapp-repo-host-key
  - name: zsalesorderfioriapp-uaa
    parameters:
      service-key:
        name: zsalesorderfioriapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zsalesorderfioriapp_html_repo_host
          ServiceInstanceName: zsalesorderfioriapp-html5-service
          ServiceKeyName: zsalesorderfioriapp-repo-host-key
          sap.cloud.service: zsalesorderfioriapp
        - Authentication: OAuth2UserTokenExchange
          Name: zsalesorderfioriapp_uaa
          ServiceInstanceName: zsalesorderfioriapp-xsuaa-service
          ServiceKeyName: zsalesorderfioriapp-uaa-key
          sap.cloud.service: zsalesorderfioriapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zsalesorderfioriapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zsalesorderfioriapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zsalesorderfioriapp.zip
      name: zsalesorderfioriapp
      target-path: resources/
- name: zsalesorderfioriapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zsalesorderfioriapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zsalesorderfioriapp-destination-service
    service-plan: lite
- name: zsalesorderfioriapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zsalesorderfioriapp-xsuaa-service
    service-plan: application
- name: zsalesorderfioriapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zsalesorderfioriapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
